<%= form_for(@amenity) do |f| %>
  <% if @amenity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@amenity.errors.count, "error") %> prohibited this amenity from being saved:</h2>

      <ul>
      <% @amenity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %><br>
    <%= f.text_field :label %>
  </div>

  <div class="form-group">
    <%= f.label :set_new_image %>

    <%= image_tag @amenity.icon %>
    <%= f.file_field :icon, style: 'display:inline-block' %>

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
